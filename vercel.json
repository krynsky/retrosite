{
  "buildCommand": "npm run demo:restore && npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/timeline/:path*", "destination": "/" },
    { "source": "/about", "destination": "/" },
    { "source": "/how-to-use", "destination": "/" }
  ]
}
